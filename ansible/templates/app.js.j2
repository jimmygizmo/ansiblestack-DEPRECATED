const express = require('express');
const app = express();
const port = 3000;


app.get('/', (req, res) => {
    res.send('Welcome from Ansible managed host:{{ inventory_hostname }}'));
  }

app.listen(port, () => {
    console.log(`Managed host demo web app listening on port ${port}!`));
  }

